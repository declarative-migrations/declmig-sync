    [package]
    name = "declmig-sync"
    version = "0.1.0"
    edition = "2021"
    license = "MIT"
    description = "opto-sync wrapper for Declarative Migrations"
    repository = "https://github.com/declarative-migrations/declmig-sync"

    [dependencies]
    serde = { version = "1", features = ["derive"] }
    thiserror = "2"

[lints.rust]
unsafe_code = "forbid"


